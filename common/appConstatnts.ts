import path from "path";

export const DS = "/";
export const ROOT_DIR = path.resolve('./');
export const ASSETS_DIR = `${ROOT_DIR}${DS}assets`;
export const BANNER_IMAGES_DIR = `${ASSETS_DIR}${DS}tickets${DS}`;